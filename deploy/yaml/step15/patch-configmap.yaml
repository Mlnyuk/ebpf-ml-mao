apiVersion: v1
kind: ConfigMap
metadata:
  name: ebpf-ml-mao-runtime
  namespace: ebpf-obs
data:
  DEPLOYMENT_STEP: "step15"
  DEPLOYMENT_IMAGE: ghcr.io/mlnyuk/ebpf-ml-mao:step15
  DEPLOYMENT_RUNBOOK: docs/steps/step15/runbook.md
  # Correct Prometheus service name for this cluster (release: my-monitoring)
  PROMETHEUS_URL: http://my-monitoring-kube-prometh-prometheus.monitoring.svc.cluster.local:9090/metrics

apiVersion: v1
kind: Service
metadata:
  name: ebpf-ml-mao-ui
  namespace: ebpf-obs
spec:
  type: ClusterIP
  selector:
    app: ebpf-ml-mao-analyzer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
